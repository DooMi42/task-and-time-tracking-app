# Database Configuration
spring.datasource.url=jdbc:postgresql://dpg-d02g143e5dus73boiv0g-a/dbtasktimemanagement_bsmv
spring.datasource.username=dbtasktimemanagement_bsmv_user
spring.datasource.password=T3E8A1wQRGZAN9S4Vl3yKoRUWQU6pix2
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

# Flyway
spring.flyway.enabled=false
spring.flyway.locations=classpath:db/migration

# JWT Configuration
jwt.secret=your_jwt_secret
jwt.expiration=3600

# Email Configuration
spring.mail.host=smtp.example.com
spring.mail.port=587
spring.mail.username=your_email@example.com
spring.mail.password=your_email_password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Logging Configuration
logging.level.org.springframework=INFO
logging.level.com.tasktracker=DEBUG

# Profile-specific settings
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}